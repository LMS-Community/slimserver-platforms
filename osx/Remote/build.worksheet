# ----------# SLIMP3 Remote Build Commands (BBEdit Worksheet)# ----------cd /Users/dave/dev/thinbitsCVS/SliMP3Controller# Bump the build number by one.agvtool bump -all# Update the version string. This appears in the About window and the Get Info window.agvtool new-marketing-version "1.0.3 (v"`agvtool vers -terse`")"# Perform a clean deployment buildpbxbuild -buildstyle Deployment clean build# Go run the built code and make sure it's good. # Double check that the version strings are as expected.# ...# Tag the source in CVS.agvtool tag -Q