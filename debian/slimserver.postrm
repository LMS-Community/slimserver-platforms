#!/bin/sh -e

if [ "$1" = purge ]; then

	rm -rf /var/cache/slimserver
	rm -rf /var/log/slimserver

	if [ -x `which deluser` ]; then
		deluser slimserver 
	fi
fi

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0

